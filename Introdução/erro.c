#include <stdio.h>

int main(void)
{
    printf("%d\n", 0678); 
    /*O valor inicia com o numero 0 o que indica que é um valor octal,
    porem ele termina com 8 e os valores em octal vão até o numero 7,
    ou seja, basta remover o 0 e escrever apenas 678.*/
    return 0;
}